db:
  image: orchardup/mysql
  ports:
    - "33306:3306"
  environment:
    MYSQL_DATABASE: dataslurp
mq:
  image: dockerfile/rabbitmq
cass:
  image: spotify/cassandra
app:
  build: .
  volumes:
    - .:/dataslurp-app
  environment:
    DB_HOST: db
  links:
    - db
    - mq
    - cass
